// Styles for buttons and text
import 'package:flutter/material.dart';

class Styles {
  static const appBarColor = Color(0xff789DBC);
  static const buttonColor = Color(0xff789DBC);
  static const backgrounColor = Color(0xffFFE3E3);
  static const heading = TextStyle(
      fontSize: 22.0, letterSpacing: 2.0, fontWeight: FontWeight.bold);

  static const timerText = TextStyle(
      fontSize: 48.0,
      letterSpacing: 2.0,
      fontWeight: FontWeight.bold,
      color: Color(0xff789DBC));

  static const lapStyle = TextStyle(
    fontSize: 24,
    color: Color(0xffFFE3E3),
  );
}
